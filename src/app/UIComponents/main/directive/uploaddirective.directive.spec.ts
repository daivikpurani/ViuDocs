import { UploaddirectiveDirective } from './uploaddirective.directive';

describe('UploaddirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new UploaddirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
